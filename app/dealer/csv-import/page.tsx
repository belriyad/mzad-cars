import { Card } from "@/components/ui/card";

export default function DealerCsvImportPage() {
  return (
    <section className="space-y-3">
      <h1 className="text-2xl font-semibold">CSV import</h1>
      <Card>Upload inventory CSV with validation and preview flow.</Card>
    </section>
  );
}
